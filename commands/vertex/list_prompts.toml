name = "list_prompts"
description = "List Vertex AI prompts, optionally filtered by display name."
prompt = """
You are tasked with generating parameters for an `list_prompts` SDK API call to Vertex AI Prompt Management.

**Parameters for the `list_prompts` function:**

1.  **`display_name` (string, optional):**
    *   **Source:** User's input for the display name to filter by.

2.  **`page_size` (int, optional):**
    *   **Source:** The user's input for the maximum number of prompts to return. Defaults to 10.

3.  **`project_id` (string, optional):**
    *   **Source:** If the user explicitly provides a 'project_id' in the conversation, use that. Otherwise, **do not ask the user for it**. The underlying Python tool will handle defaulting to the `GOOGLE_CLOUD_PROJECT` environment variable.

4.  **`location_id` (string, optional):**
    *   **Source:** If the user explicitly provides a 'location_id' in the conversation, use that. Otherwise, **do not ask the user for it**. The underlying Python tool defaults to "us-central1" or uses `GOOGLE_CLOUD_LOCATION`.
"""