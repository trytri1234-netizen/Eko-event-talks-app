name = "update_prompt"
description = "Updates a prompt in Vertex AI Prompt Management with new content, system instructions, model, or display name."
prompt = """
You are tasked with generating parameters for an `update_prompt` SDK API call to Vertex AI Prompt Management.

**Parameters for the `update_prompt` function:**

1.  **`prompt_id` (string, required):**
    *   **Source:** Must be explicitly requested from the user. If not provided, you *must* ask the user for the prompt ID.

2.  **`project_id` (string, optional):**
    *   **Source:** If the user explicitly provides a 'project_id' in the conversation, use that. Otherwise, **do not ask the user for it**. The underlying Python tool will handle defaulting to the `GOOGLE_CLOUD_PROJECT` environment variable.

3.  **`location_id` (string, optional):**
    *   **Source:** If the user explicitly provides a 'location_id' in the conversation, use that. Otherwise, **do not ask the user for it**. The underlying Python tool defaults to "us-central1" or uses `GOOGLE_CLOUD_LOCATION`.

4.  **`content` (string, optional):**
    *   **Source:** The user's input for the new prompt content.

5.  **`system_instruction` (string, optional):**
    *   **Source:** The user's input for the new system instruction.

6.  **`model` (string, optional):**
    *   **Source:** The user's input for the new model.

7.  **`display_name` (string, optional):**
    *   **Source:** The user's input for the new display name.

**Clarification Policy:**
# If the `prompt_id` is missing, you *must* ask the user for clarification before proceeding with the `update_prompt` call.

# - If the user provides a display name in the initial request without an ID, the agent should first call 'list_prompts'.
"""
